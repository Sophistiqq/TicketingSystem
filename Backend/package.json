{
  "name": "aio",
  "version": "1.0.50",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "migrate-dev": "bun run migrate dev",
    "migrate": "bun prisma migrate dev --name init && bun prisma generate",
    "migrate:deploy": "bun prisma migrate deploy && bun prisma generate",
    "build": "bun install && bun prisma migrate deploy && bun prisma generate && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile server src/index.ts",
    "start": "./server"
  },
  "dependencies": {
    "@elysiajs/cookie": "^0.8.0",
    "@elysiajs/cors": "^1.4.1",
    "@elysiajs/jwt": "^1.4.0",
    "@elysiajs/swagger": "^1.3.1",
    "@prisma/adapter-better-sqlite3": "^7.2.0",
    "@prisma/adapter-libsql": "^7.2.0",
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3",
    "elysia": "^1.4.22",
    "prismabox": "^1.1.26"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "prisma": "^7.2.0"
  },
  "module": "src/index.js"
}
