services:
  # Frontend Static Site
  - type: web
    name: ticketing-frontend
    env: static
    rootDir: Frontend
    buildCommand: bun install && bun run build
    publishPath: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: ticketing-backend
          type: web
          property: host
      - key: NODE_ENV
        value: production
    autoDeploy: true

  # Backend Web Service
  - type: web
    name: ticketing-backend
    env: node
    rootDir: Backend
    buildCommand: bun install && bun run build
    startCommand: ./server
    disk:
      name: ticketing-uploads
      mountPath: /opt/render/project/src/Backend/uploads
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ticketing-db
          property: connectionString
      - key: FRONTEND_URL
        fromService:
          name: ticketing-frontend
          type: web
          property: host
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
    autoDeploy: true

databases:
  - name: ticketing-db
    plan: free
